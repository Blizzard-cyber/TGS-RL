# A10 工程与 Helm 验收记录

## 结论

2026-09-14，TGS-RL 在单张 NVIDIA A10 的 Kubernetes 测试机上完成工程验收：

- `make gpu-a10-readiness`：`PASSED`；
- A10 Full lifecycle：`PASSED`，真实 `checkpoint → offload → reload → resume`；
- H2 HAMi 双 worker：`PASSED`，两个 worker 共享同一物理 GPU；
- 恢复 NVIDIA Device Plugin 后 E1：`PASSED`；
- 六服务 NVIDIA Helm smoke：`PASSED`，315 个证据文件哈希全部匹配；
- 最终无测试 Job、ResourceClaimTemplate、ResourceClaim 或 JobRunBundle 残留。

这仍是单节点最小 trainer 的工程验证，不代表完整 veRL 训练、性能收益、MIG、多节点或
GPU Pod/节点/网络故障恢复已经验证。

## 验证基线

| 项目 | 实际值 |
|---|---|
| 仓库与镜像基线 | `02951329b4f4049660590584b46602f646ad023f` |
| GPU | NVIDIA A10，23028 MiB |
| NVIDIA 驱动 | `580.178.04` |
| Kubernetes | `v1.35.1` |
| Minikube | `v1.38.1` |
| Helm | `v4.2.4` |
| A10/H2/E1 evidence | `GPU_SINGLE_NODE`，`simulated=false` |

Helm 最终通过时，控制面和 workload 镜像均来自上述 commit 的不可变 digest。实装过程中发现
并使用当前工作树修复了三个部署/重跑缺陷：

1. 新 hardware campaign 与持久化 Gateway 复用旧 idempotency key；
2. Kubernetes 1.35 不支持 `kubectl rollout status deployment --all`；
3. 非 root Scheduler 无法对 root-owned PVC 挂载点执行 `chmod`，helper 状态必须写入独立子目录。

因此 Helm 原始报告中的 `git_commit` 仍是镜像基线 `0295132`；最终 Helm 编排同时依赖本记录
对应工作树中的 `scripts/hardware_environment_driver.py`、`scripts/gpu-helm-smoke.sh` 和
`deploy/helm/tgsrl/templates/scheduler.yaml` 修复。不能把该报告误解为未应用这些修复的
`0295132` 原样通过。

## A10 总体验收

`.cache/tgsrl/a10-readiness/readiness-summary.json` 的四个组成部分均为 `PASSED`：

| 组成部分 | 结果 |
|---|---|
| 工程故障路径 | `CPU_REAL_PROCESS_AND_COMPONENT_INTEGRATION`，7 类检查通过 |
| A10 Full lifecycle | `GPU_SINGLE_NODE`，8 次执行 |
| H2 HAMi 双 worker | `GPU_SINGLE_NODE`，8 次执行 |
| DRA 恢复 E1 | `GPU_SINGLE_NODE`，8 次执行 |

A10 lifecycle 的三轮 measurement 均观测到：

| 动作 | allocated bytes | reserved bytes |
|---|---:|---:|
| offload | `545390592 → 8519680` | `562036736 → 20971520` |
| resume | `8519680 → 545390592` | `20971520 → 557842432` |

每次 offload 均有 checkpoint，resume 均恢复 ready。H2 variant 的两个 worker 聚合份额为
`0.8`，最大执行重叠为 `4004.587 ms`。恢复 DRA 后 E1 的 8 次执行全部通过。

## Helm 验收

六个控制面 Deployment 均达到 `1/1 Ready`，并全部使用不可变
`repository@sha256:...` 镜像。Helm 下的 A10 lifecycle 同样执行 8 次并得到
`PASSED`；三轮 measurement 的 allocator bytes 都满足 offload 下降、resume 回升。

最终证据目录为测试机 `.cache/tgsrl/helm-smoke/`：

- `evidence-index.json`：`PASSED`，315 个文件；
- 索引内 315 个 SHA-256 与文件内容、大小逐项匹配；
- evidence 扫描未发现 registry signing key、Bearer token 或私钥；
- port-forward 与 campaign 进程均已退出；
- Helm release `tgsrl` 保持 deployed，六个控制面 Pod 健康。

## 关键哈希

| 文件 | SHA-256 |
|---|---|
| `a10-readiness/readiness-summary.json` | `42b804e4421ae41c857ae38e75a193b6f177fd8071d3482aa8d797d4c6b8753a` |
| `a10-readiness/campaign-report.json` | `dc732efb68adb25a379000d40fb0de4ed7f7d73513e4f8cc06c133bc18c46bcc` |
| `a10-readiness/a10-full-lifecycle/report.json` | `6f3d1259b375f858b5bcaaa0eb8584ef1f6358529b1168256a869ff1052bdc87` |
| `hami-concurrency-smoke/campaign-report.json` | `8db7dbb37c421cec87e603fe63ff3b495b8d72d32dbf9170ae5679b74810c61c` |
| `hami-concurrency-smoke/h2-hami-concurrency/report.json` | `62f0fddf0a862c91954c1c59d6aa2ba26b0122f49145518a5dc715a685ffafb5` |
| `gpu-smoke/campaign-report.json` | `7afefbaee5b5e1df1b620a8419f5e80afc2809db4214357c49bc330a1fe00041` |
| `gpu-smoke/e1-full-gpu/report.json` | `1d1b4f94d6ea1b069f04c695169e5ea70e91b0112c92cc83191901f1e1b0cde4` |
| `helm-smoke/evidence-index.json` | `ca5ef1e8efdefa6f8349fe97755b5bee2f9f335ac5729ef40ce2db1a6f86fc58` |
| `helm-smoke/a10-readiness/a10-full-lifecycle/report.json` | `dcde8e890674aead97a12baff901c678638a0f8bdbf3d605567004c331958b0a` |
| `helm-smoke/rendered.yaml` | `fd0f09bc2bf1aeba936a89257d526f9f33e6d202330c56560cb074f9c767d6d6` |

原始证据保留在测试机 `.cache/tgsrl/`，不提交 kubeconfig、signing key、私有地址或原始日志。

## 未覆盖范围

- A10 不提供本项目可用的 MIG 拓扑，MIG 为 `NOT_RUN`；
- 单节点环境不覆盖多节点；
- GPU Pod 强制退出、容量耗尽、节点或网络丢失仍为 `NOT_RUN`；
- workload 是最小 adapter trainer，不是完整 veRL trainer/collective；
- 本轮不做 E3–E8 性能标定，也不宣称吞吐或 VUG 收益。
