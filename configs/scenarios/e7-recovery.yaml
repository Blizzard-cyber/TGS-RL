{
  "schema_version": "tgsrl.io/hardware-scenario/v1alpha1",
  "experiment_id": "E7",
  "title": "Failure injection and transactional recovery",
  "topology": {"minimum_nodes": 1, "minimum_accelerators": 1, "gpu_profiles": ["full-gpu", "mig"]},
  "workload": {"restart_components": ["runtime", "scheduler", "operator", "worker"]},
  "faults": [{"fault_id": "worker-exit", "kind": "process-exit"}, {"fault_id": "control-response-loss", "kind": "response-drop"}],
  "required_evidence": ["fault-injected", "durable-receipt", "rollback", "fault-recovered"]
}
