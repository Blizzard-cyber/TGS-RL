{
  "$schema": "https://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "image": {
      "type": "object",
      "properties": {
        "digest": {
          "type": "string",
          "pattern": "^(|sha256:[a-f0-9]{64})$",
          "description": "Immutable image digest. Required for production; an empty value permits the local-development tag fallback."
        }
      }
    }
  }
}
